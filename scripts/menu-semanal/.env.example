# Variables de entorno para los scripts de cerebro
# Copiar a .env y rellenar los valores reales
# NUNCA subir .env a git

LOG_LEVEL=INFO

# === LLM ===
LLM_API_KEY=
LLM_MODEL=claude-haiku-4-5

# === MCP Vault ===
MCP_VAULT_URL=https://<tailscale-host>:3100
MCP_VAULT_ROOT=/vault
