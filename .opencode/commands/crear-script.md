Implementa el script descrito en la spec `docs/specs/$ARGUMENTS.md`.

Sigue estos pasos:
1. Lee `docs/specs/$ARGUMENTS.md` en su totalidad.
2. Lee `docs/profiles/behaviors/new-script.md` para seguir las convenciones del proyecto.
3. Implementa el script en `scripts/$ARGUMENTS.py` siguiendo la estructura obligatoria definida en el perfil.
4. Al terminar, actualiza `scripts/README.md` añadiendo el nuevo script con su propósito y cron schedule.
5. Muéstrame el script completo y la línea de cron antes de dar por terminado.
